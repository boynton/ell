(use sort)
(use pi)

(define (make-foo n) (range n))

(define (benchmark)
  (let ((foo (make-foo 100000)))
    (sort foo <)
    (sort foo >)
    (pi 1000 5)))

(let loop ((i 5))
  (if (> i 0)
      (do
        (benchmark)
        (loop (- i 1)))))
